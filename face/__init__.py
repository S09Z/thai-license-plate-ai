"""Face detection for the camera overlay.

Detection only: this package locates face regions in a frame and reports
their coordinates. It does not identify anyone, compute embeddings, match
against a gallery, or persist frames.
"""
